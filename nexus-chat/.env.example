# Copy to .env.production for production builds or .env.local for local dev
# For Render Static Site builds, this can be set in the service env vars
REACT_APP_SOCKET_URL=https://your-backend.example.com
